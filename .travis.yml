language: cpp

compiler:
  - gcc
  - clang

# Change this to your needs
after_script: 
  - "make test"

before_script:
  - sudo apt-get -qq install libboost-all-dev

script: cmake . && make


after_script: 
  - "make test"
